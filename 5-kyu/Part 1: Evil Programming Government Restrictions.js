var anarchy = function () {
  return 'I can write numbers like, ' + ('b'.charCodeAt() - 'a'.charCodeAt()).toString() + ', ' + ('c'.charCodeAt() - 'a'.charCodeAt()).toString() + ', ' + ('d'.charCodeAt() - 'a'.charCodeAt()).toString() + '.';
}
